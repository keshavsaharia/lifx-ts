import {
	UIElement
} from '../ui'

import Request from '../request'

export default class GetRequest extends Request {

	// private render(element: UIElement): GetRequest {
	// 	this.responded = true
	// 	this.response.writeHead(200, {
	// 		'Content-Type': 'text/html',
	// 		'Keep-Alive': 'true'
	// 	})
	// 	this.response.end(element.render())
	// 	return this
	// }

}
